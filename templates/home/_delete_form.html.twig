<form class="w-137px" method="post" action="{{ path('app_user_delete', {'id': app.user.id}) }}" onsubmit="êtes-vous sûr de vouloir supprimer votre compte ?">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ app.user.id) }}">
    <button class="b-log">Me désinscrire</button>
</form>
